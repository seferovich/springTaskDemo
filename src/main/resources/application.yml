security:
  jwt:
    secret-key: "verysecretkeyomg"

spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/jwt_security
    username: isa
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8090

